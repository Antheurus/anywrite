# Vendored proto files

The `.proto` files in this directory are sourced from
[anyproto/anytype-heart](https://github.com/anyproto/anytype-heart), Anytype's
middleware gRPC service definitions, under the Any Source Available License 1.0.
Vendored here for personal, non-commercial use only (Anytype's own internal
middleware API, used to embed images as real inline blocks — a capability the
public Local JSON API does not expose). Not redistributed as part of a
commercial product.
